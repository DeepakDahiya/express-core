diff --git a/chrome/android/java/src/org/chromium/chrome/browser/toolbar/ToolbarManager.java b/chrome/android/java/src/org/chromium/chrome/browser/toolbar/ToolbarManager.java
index 46ae70839739bf0e6ad8ac1973c0b3774f44fcdd..8924c0d3aac61266aa70e756af970ff622d4a9a0 100644
--- a/chrome/android/java/src/org/chromium/chrome/browser/toolbar/ToolbarManager.java
+++ b/chrome/android/java/src/org/chromium/chrome/browser/toolbar/ToolbarManager.java
@@ -835,6 +835,13 @@ public class ToolbarManager implements UrlFocusChangeListener, ThemeColorObserve
                 updateTabLoadingState(true);
                 onBackPressStateChanged();
 
+                // NewTabPage ntp = getNewTabPageForCurrentTab();
+                // if (ntp != null) {
+                //     mLocationBar.getContainerView().setVisibility(View.GONE);
+                // } else {
+                //     mLocationBar.getContainerView().setVisibility(View.VISIBLE);
+                // }
+
                 // A URL update is a decent enough indicator that the toolbar widget is in
                 // a stable state to capture its bitmap for use in fullscreen.
                 mControlContainer.setReadyForBitmapCapture(true);
@@ -1864,6 +1871,18 @@ public class ToolbarManager implements UrlFocusChangeListener, ThemeColorObserve
     public void onUrlFocusChange(boolean hasFocus) {
         mToolbar.onUrlFocusChange(hasFocus);
 
+        // if (hasFocus) {
+        //     mLocationBar.getContainerView().setVisibility(View.VISIBLE);
+        // } else {
+        //     NewTabPage ntp = getNewTabPageForCurrentTab();
+        //     if (ntp != null) {
+        //         mLocationBar.getContainerView().setVisibility(View.GONE);
+        //     } else {
+        //         mLocationBar.getContainerView().setVisibility(View.VISIBLE);
+        //     }
+        // }
+        
+
         if (mIsStartSurfaceRefactorEnabled && mLayoutStateProvider != null
                 && mLayoutStateProvider.isLayoutVisible(LayoutType.START_SURFACE)) {
             mToolbar.updateStartSurfaceToolbarState(null, !hasFocus, LayoutType.START_SURFACE);
